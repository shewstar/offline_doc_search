"""Offline PDF search — Phase 1 proof of concept.

Single Python runtime. Phase 1 ships a CLI over the same core modules
(db / extractor / indexer / search) that the later FastAPI app will reuse.
"""
